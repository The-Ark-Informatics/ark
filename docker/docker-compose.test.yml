version: '2'
services:
        test:
                build: test
                depends_on:
                        - tomcat
                links:
                        - tomcat
                        - mysql
                volumes:
                        - ./test/output:/output
                env_file: env_file

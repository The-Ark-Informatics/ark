# If you change any of these after the initial 'docker-compose build' or 'docker-compose up' you'll
# need to tear down all the containers, and delete their volumes (easily done with 'docker-compose down -v')
# Note: Doing this will delete all contents of your MySQL and LDAP databases.

MYSQL_ROOT_PASSWORD=mysql-password
SLAPD_PASSWORD=slapd-password
ARK_SUPERUSER_PASSWORD=Password_1
ARK_USERNAME=arksuperuser@ark.org.au

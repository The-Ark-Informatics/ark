import java.util.*;
Vector groups = new Vector();
		groups.add("3");
		groups.add("5");
		groups.add("45");
    	String inString="";
    	if (groups.size() == 0)
    		inString = "(null)";
    	else { 
    		inString = "(";
    	for (int i=0; i < groups.size(); i++) {
    		if (i != 0) 
    			inString +=",";
    		inString += (String) groups.get(i);
    	}
    		inString += ")";
    	}
    	System.out.println(inString);
    	